num_views: 4 # ! faster pre-training

fixed_length: True # as in https://github.com/CUT3R/CUT3R/blob/main/config/stage3.yaml
allow_repeat: True
n_corres_train: 0
n_corres_test: 0

# 

resolution: ${resolution_stage1} # for smaller vram

dataset0: Co3d_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_co3d/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset2: WildRGBD_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_wildrgbd_mp", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

dataset3: ARKitScenes_Multi(allow_repeat=${allow_repeat}, split='train', ROOT='${dataset_root}/processed_arkitscene/', aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset3_stage1: ARKitScenes_Multi(allow_repeat=${allow_repeat}, split='train', ROOT='${dataset_root}/processed_arkitscene/', aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset4: ARKitScenesHighRes_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_arkitscene_highres", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
# dataset4: ARKitScenesHighRes_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root_cpfs}/processed_arkitscene_highres", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset5: ScanNetpp_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_scannetpp_fix/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset6: ScanNet_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_scannet/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset7: HyperSim_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_hypersim", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

dataset8: BlendedMVS_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_blendedmvs/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset9: MegaDepth_Multi(allow_repeat=${allow_repeat}, split="train", ROOT="${dataset_root}/processed_megadepth", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset10: MapFree_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_mapfree/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset11: Waymo_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_waymo/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset12: VirtualKITTI2_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_vkitti", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset13: UnReal4K_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_unreal4k/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset14: TartanAir_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_tartanair/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

dataset15: DL3DV_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_dl3dv", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

dataset16: Cop3D_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_cop3d/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
# dataset17: MVImgNet_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_mvimgnet/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset17: MVImgNet_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_mvimgnet_new/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset18: RE10K_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_re10k/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset19: OmniObject3D_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_omniobject3d/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

dataset20: ThreeDKenBurns(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_3dkb/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset21: IRS(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_irs/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset22: SynScapes(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_synscapes/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset23: UrbanSyn(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_urbansyn/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset24: EDEN_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_eden", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset25: SmartPortraits_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_smartportraits", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

dataset26: DynamicReplica(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_dynamic_replica/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset27: Spring(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_spring/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset28: BEDLAM_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_bedlam", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset29: MVS_Synth_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_mvs_synth", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset30: PointOdyssey_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_pointodyssey", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

dataset33: HOI4D_Multi(allow_repeat=True, split=None, ROOT="${dataset_root}/processed_hoi4d",
  aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

# dataset31: UASOL_Multi(allow_repeat=${allow_repeat}, split='train', ROOT="${dataset_root}/processed_uasol", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})
dataset32: MP3D_Multi(allow_repeat=${allow_repeat}, split=None, ROOT="${dataset_root}/processed_mp3d/", aug_crop=16, resolution=${resolution}, transform=SeqColorJitter, num_views=${num_views}, n_corres=${n_corres_train})

